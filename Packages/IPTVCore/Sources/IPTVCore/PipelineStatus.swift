/// Phase 0 placeholder proving the app target links against the local IPTVCore package.
/// Real domain/data/persistence/download code lands here starting Phase 1.
public enum PipelineStatus {
    public static let message = "Zero-Mac build pipeline: OK"
}
